Esta carpeta contiene las simulaciones (pruebas de performance) del proyecto.

CONSIDERACIONES:

El Package de simulation debe contener lo siguiente:

    Componente (Package): bffAccount
		    i. endPath.scala —>  DebitBalance.scala

    NOTA: dentro del file.scala debes configurar los path para que se puedad correr sin problemas la prueba de performance

En caso de dudas favor revisar y mantener estructura presentada en template que esta en la ruta ruta:

src/test/java/simulations/template/componente/endPath.scala
